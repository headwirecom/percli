#!/usr/bin/env node

const tasks =  require('../lib/tasks')

const args    = require('commander')
const banner  = require('../lib/banner.js')

args.version('1.0.0')
    .parse(process.argv)

banner.banner()

tasks.startPeregrine()